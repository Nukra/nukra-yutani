# Screenshots

Real captures, all on the `ice` preset:

- `desktop-ice.png` — the session, wallpaper and panel. README hero.
- `overview-ice.png` — the app grid. Also the honest picture of icon coverage:
  the drawn glyphs are square and two-tone, and the apps that fall through to
  Adwaita keep their full-colour icons.
- `terminal-ice.png` — Ptyxis with the boot banner.

`desktop-green.png` is a **schematic** exported from `docs/index.html` — drawn,
not photographed — kept as a fallback.

Still missing:

- quick settings open
- Files, showing role colouring on a mixed directory
- the login screen
- `paper` alongside a dark preset

Name them `<what>-<preset>.png`. Wayland captures cleanly with
`gnome-screenshot` or the shell's own Print Screen.
